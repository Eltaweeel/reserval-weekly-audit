{
  "name": "reserval-weekly-audit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "test:headed": "playwright test --config=playwright.config.ts --headed",
    "report": "playwright show-report artifacts/playwright-html-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.2",
    "typescript": "^5.3.3"
  }
}
